name: build

on:
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  tox:
    uses: ./.github/workflows/tox.yml
    secrets: inherit
  docker:
    uses: ./.github/workflows/docker.yml
    secrets: inherit
